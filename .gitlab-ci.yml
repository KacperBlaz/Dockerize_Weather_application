stages:
  - test


test:
  image: docker:20-git
  services:
    - docker:20-dind
  variables:
    TABLE_NAME: "Pipeline_test"
    DOCKER_TLS_CERTS: "/certs"
  script:
    - env
    - cd $PROJECT_PATH
    - docker-compose up &